%li.link
  .node
    - if link.bookmark?
      = image_icon(:page_link)
    - elsif link.folder?
      = image_icon(:folder_link)
    - else
      = image_icon(:link)
    %i= link_to link.title, :anchor => "entry-#{ link.id }"
    .actions
      = link_to image_icon(:delete), '', :title => '...'
