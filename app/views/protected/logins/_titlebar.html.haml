%li.menu
  - link_to '#current' do
    .bold.label= title
%li#current.hidden
  %ul
    %li= link_to t('actions.quit'), login_path
%li.menu
  - link_to '#logins' do
    .label= t('actions.protected.logins.index')
%li#logins.hidden
  %ul
    %li= link_to t('actions.protected.logins.new'), new_protected_login_path(:return_uri => current_uri)
    %li= link_to t('actions.protected.logins.index'), protected_logins_path
    %li= link_to t('actions.print'), 'javascript: window.print();'
%li.menu
  - link_to '#search' do
    .label= t('actions.search')
%li#search.hidden
  - form_for [:protected, @searcher], :html => { :method => :get } do |f|
    %p
      = f.text_field :q
      %a.submit{ :href => '' }= "#{ icon :search }#{ t 'actions.go' }"
