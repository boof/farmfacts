de:
  activerecord:

    errors:

      template:
        header:
          #one: '{{model}}'
          #other: '{{model}}{{count}}'
        #body:

      models:
        #login:
          #attributes:
            #username:
              #blank:
            #password:
              #blank:

    attributes:

      group:
        name:   Name
        desc:   Beschreibung
        nodesc: keine Beschreibung vorhanden

      login:
        username:               Benutzername
        first_name:             Vorname
        last_name:              Nachname
        email:                  E-Mail
        password:               Passwort
        password_confirmation:  Passwort Bestätigung
        nogroups:               kein Mitglied einer Gruppe

    models:
      role:               Rolle
      belonging_role:     Mitglied
      leading_role:       Leiter
      administering_role: Administrator
      norole:             Keine Rolle

